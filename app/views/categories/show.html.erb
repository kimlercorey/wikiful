<div>
  <h1><%= "Articles Related to #{@category.name}" %></h1>
  <% @articles.each do |a| %>
    <p>
      <div><%= link_to a.title, a, {:class => "lead"} %> </div>
      <div> <%= "#{a.content[0...100]}..." %> </div>
      <small> published: <%= a.created_at.strftime('%b %d, %Y') %> </small>
    </p>
  <% end %>
  </div>
